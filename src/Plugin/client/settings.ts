

export const settings = {
  // Can't use keys like access_token, the chrome storage won't handle them correctly
  storageKeys: {
    accessTokenKey: 'accessToken',
    isLoggedInKey: 'isLoggedIn',
    pluginIdKey: 'pluginId',
    Version: "Ok",
  }
};

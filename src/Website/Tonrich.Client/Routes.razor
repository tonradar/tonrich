<CascadingAuthenticationState>
    <LayoutView Layout="@typeof(MainLayout)">
        <Router AppAssembly="@GetType().Assembly"
                AdditionalAssemblies="@(AssemblyLoadContext.Default.Assemblies.Where(asm => asm.GetName().Name?.Contains("Tonrich") is true))"
                OnNavigateAsync="@OnNavigateAsync">
            <Found Context="routeData">
                <AuthorizeRouteView RouteData="@routeData">
                    <Authorizing>
                        <LoadingComponent Color="#FF5C11" />
                    </Authorizing>
                    <NotAuthorized>
                        <NotAuthorizedComponent />
                    </NotAuthorized>
                </AuthorizeRouteView>
            </Found>
            <NotFound>
                <NotAuthorizedComponent />
            </NotFound>
            <Navigating>
                <LoadingComponent Color="#FF5C11" />
            </Navigating>
        </Router>
    </LayoutView>
</CascadingAuthenticationState>